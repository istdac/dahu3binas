import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Proyecto-blank',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
